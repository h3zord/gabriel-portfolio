'use client'

import styled from 'styled-components'

export const HeroImageTravel = styled.main`
  width: 100vw;
  height: 110vh;

  & > img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
`
