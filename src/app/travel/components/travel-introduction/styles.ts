'use client'

import styled from 'styled-components'

export const HeroImageTravel = styled.main`
  width: 100vw;
  height: 100vh;
  position: relative;

  & > img {
    object-fit: cover;
    object-position: 50% 100%;
  }
`
