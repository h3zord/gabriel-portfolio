'use client'

import styled from 'styled-components'

export const HeroImageHotel = styled.main`
  width: 100vw;
  height: 100vh;

  & > img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
`
